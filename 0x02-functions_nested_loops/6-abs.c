/**
 * _abs - Calculates the absolute value of n
 * @n: The integer to be converted into its absolute value
 *
 * Return: The absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
